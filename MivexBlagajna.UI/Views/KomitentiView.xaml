<UserControl x:Class="MivexBlagajna.UI.Views.KomitentiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MivexBlagajna.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="0" Background="LightGray">
            <TextBlock Text="Lista komitenata" Margin="10" Background="White"/>
            <ListView Margin="10 0">
                <ListViewItem Content="Komitent 1" />
                <ListViewItem Content="Komitent 2" />
                <ListViewItem Content="Komitent 3" />
            </ListView>
        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="1" Margin="0 10 0 0">
            <Grid.RowDefinitions>

                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition />

            </Grid.RowDefinitions>

            <TextBlock Text="Detalji" Grid.Row="0" Margin="10"/>

            <Label Content="Sifra: " Grid.Row="1" />
            <TextBox Grid.Row="2" Width="50" HorizontalAlignment="Left" Margin="5 0 0 0"
                     Text="{Binding SelectedKomitent.Sifra}"/>

            <StackPanel Orientation="Horizontal" Grid.Row="5">
                <Label Content="Naziv: " Grid.Row="3" Margin="0 10 0 0"/>
                <Label Content="Naziv 2: " Grid.Row="3" Margin="120 10 0 0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Row="6">
                <TextBox Width="150" HorizontalAlignment="Left" Margin="5 0 0 0" Text="{Binding SelectedKomitent.Naziv}"/>
                <TextBox Width="150" HorizontalAlignment="Left" Margin="15 0 0 0" Text="{Binding SelectedKomitent.Naziv2}"/>
            </StackPanel>
        </Grid>
        
    </Grid>
</UserControl>
